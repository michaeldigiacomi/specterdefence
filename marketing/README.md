# SpecterDefence Marketing Site

Static marketing/landing site (React + Vite), served by Nginx from `k8s/prod/marketing.yaml` at `specterdefence.digitaladrenalin.net`.

```bash
npm install
npm run dev    # local dev (HMR)
npm run build  # production build
```

CI workflow: `.github/workflows/marketing.yml`.
